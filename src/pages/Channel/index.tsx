import Workspace from '@layouts/Workspace/Index';
import React from 'react';

const Channel = () => {
  return (
    <Workspace>
      <div>hello</div>
    </Workspace>
  );
};

export default Channel;
